package players;

import gamePieces.Board;
import gamePieces.Rack;
import gamePieces.TileBag;

public class ComputerPlayer extends Player {
    public ComputerPlayer(Board board, TileBag tileBag) {
        super(board, tileBag);
    }
}
